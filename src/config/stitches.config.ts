import { createStitches } from '@stitches/react'

export const { styled } = createStitches({
  theme: {
    colors: {
      background: '#000500',
      backgroundLight: '#292929',

      primary: '#f96e46',
      secondary: '#473bf0',
    },
  },
})
